// Lazy-loaded via React.lazy in Portfolio.tsx — Vite code-splitting pattern.
// In production: render a modal or expanded project view here.
// Currently returns null — placeholder for future implementation.
export default function PortfolioModal() {
  return null
}
